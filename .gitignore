.env
node_modules/

# Recorded data stays on the machine that recorded it.
*.csv
*.json.bak

# The one exception: a synthetic file, committed so the app can be tried
# with no headset. Generated by `npm run sample`.
!web/sample-session.csv
